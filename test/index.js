const expect = require('chai').expect;
const InfiniteScroll = require('../dist/main').InfiniteScroll;

describe('scroll test', () => {
  it('initialize', () => {
    console.log(InfiniteScroll);
    const is = new InfiniteScroll()
  })
})